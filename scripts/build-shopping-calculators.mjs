import {readFile,writeFile} from 'node:fs/promises';
import {load} from 'cheerio';
const definitions=[{
 slug:'discount-calculator',name:'Discount Calculator',title:'Discount Calculator — Sale Prices & Total Savings | Kalika',description:'Calculate a sale price and your savings with a discount calculator. Apply sequential discounts and a fixed reduction, see each step, and copy your results.',h1:'Calculate Sale Prices and Discount Savings',intro:'Check what you will pay after a sale discount. Add a second percentage discount or a fixed reduction, then see the final price, total saving and each calculation step.',
 steps:['Enter the original listed price and choose a display currency.','Enter the first percentage discount.','Optionally add a second percentage discount and a fixed reduction.','Select Calculate savings to see the final price and breakdown.','Copy the summary if you want to keep the calculation.'],
 benefits:['See the effect of sequential percentage discounts.','Include a fixed reduction after percentage discounts.','Read a breakdown with amounts rounded to cents.','Calculate locally without signup or saving a price history.'],
 explainer:'A percentage discount reduces the price by the rate you enter. A second discount applies to the remaining price, not the original amount. For example, 20% off 100.00 leaves 80.00; another 10% removes 8.00, leaving 72.00. A further fixed reduction of 5.00 gives a final price of 67.00 and a total saving of 33.00.',
 faqs:[['Are two discounts added together?','No. They are applied in sequence. A 20% discount followed by 10% gives a 28% saving before rounding, not 30%. The breakdown shows the amount removed at each step.'],['Where does a fixed reduction apply?','The fixed reduction is subtracted after both percentage discounts. It cannot exceed the remaining price. Store promotions may use a different order or restrict combinations, so enter only the discounts that apply to your purchase.'],['Does this add tax or delivery charges?','No. It discounts the listed price you enter. If that price already includes tax, the discounted price remains on the same basis; the tool does not separately allocate tax. Use Sales Tax & VAT Calculator for a separate tax breakdown.'],['How precise are the results?','Prices and fixed reductions support two decimal places up to 1,000,000. Discount rates support up to four decimal places, from 0 to 100%. Each discount amount is rounded to the nearest cent, with half a cent rounded up. The effective saving is displayed to two decimal places.'],['Are my shopping amounts stored or sent elsewhere?','No. The discount calculator processes inputs in this browser tab and does not upload them or save calculation history. Copy summary uses your clipboard only when you press it. Currency selection changes formatting, not exchange rates.']],
 related:[['sales-tax-calculator','Sales Tax & VAT Calculator'],['percentage-calculator','Percentage Calculator'],['tip-calculator','Tip & Bill Split Calculator']]
},{slug:'sales-tax-calculator',name:'Sales Tax & VAT Calculator',title:'Sales Tax & VAT Calculator — Add or Remove Tax | Kalika',description:'Add sales tax or VAT to a net price, or remove included tax from a total. Enter your own rate and see the net amount, tax and final price with a breakdown.',h1:'Add Sales Tax or Calculate VAT Included in a Price',intro:'Work out a price including tax, or separate the tax already included in a total. Enter your own rate to see the amount before tax, the tax amount and the final price.',
 steps:['Choose Add tax or Remove included tax.','Enter the price before tax or including tax, as shown by the input label.','Enter the applicable tax percentage and select a display currency.','Select Calculate tax to see the three-part breakdown.','Copy the summary if you need to keep the result.'],
 benefits:['Calculate both tax-exclusive and tax-inclusive prices.','Use a custom rate with up to four decimal places.','Keep the net amount plus tax equal to the final total.','Calculate in your browser without an account.'],
 explainer:'To add tax, multiply the price before tax by the rate and add the rounded tax amount. For a price of 100.00 and an example rate of 20%, tax is 20.00 and the total is 120.00. To remove included tax, divide the total by 1 plus the rate expressed as a decimal: 120.00 ÷ 1.20 = 100.00. Subtracting 20% directly from 120.00 would give a different answer.',
 faqs:[['What is the difference between adding and removing tax?','Add tax starts with a net price and increases it by the tax amount. Remove included tax starts with a gross total, divides by 1 plus the rate, and shows the tax already included. Choose the mode that matches your receipt or quote.'],['Which country or state tax rate should I enter?','Enter the rate applicable to your transaction, obtained from your receipt or the relevant tax authority. This calculator does not select rates by location or determine taxability, exemptions or filing obligations. Example calculations do not recommend a rate.'],['Can I use a combined rate or compound taxes?','You can enter a combined rate when all components apply to the same price base. The tool applies one rate once; it does not apply one tax on top of another, handle mixed-rate baskets or allocate item-specific exemptions.'],['Why can a receipt differ by a cent?','This tool rounds one entered amount to two decimal places. Some receipts round each item or tax component separately. For included tax, the net price is rounded first and tax is the difference from the entered total, so the amounts add up exactly.'],['What limits and privacy rules apply?','Enter prices up to 1,000,000 with two decimal places and rates from 0 to 100% with up to four decimal places. Use a dot or comma for decimals, without thousands separators. Calculations run locally without saving input history; currency selection does not convert amounts.']],
 related:[['discount-calculator','Discount Calculator'],['tip-calculator','Tip & Bill Split Calculator'],['percentage-calculator','Percentage Calculator']]
}];
const currency=`<label>Display currency<select id="currency">${[['USD','US dollar'],['EUR','Euro'],['GBP','British pound'],['CAD','Canadian dollar'],['AUD','Australian dollar'],['CHF','Swiss franc']].map(([v,n])=>`<option value="${v}">${v} — ${n}</option>`).join('')}</select><span class="hint">Formatting only; no currency conversion.</span></label>`;
const reference=await readFile('public/tools/tip-calculator.html','utf8');
for(const d of definitions){const $=load(reference),url='https://kalikatools.com/tools/'+d.slug,isDiscount=d.slug==='discount-calculator';
 $('title').text(d.title);$('meta[name=description]').attr('content',d.description);$('link[rel=canonical]').attr('href',url);$('meta[property="og:url"]').attr('content',url);for(const p of ['og','twitter']){$(`meta[property="${p}:title"],meta[name="${p}:title"]`).attr('content',d.title);$(`meta[property="${p}:description"],meta[name="${p}:description"]`).attr('content',d.description);}
 $('script[src="/js/tip-calculator.js"]').attr('src','/js/shopping-calculators.js');
 $('script[type="application/ld+json"]').text(JSON.stringify({'@context':'https://schema.org','@graph':[{'@type':'WebApplication',name:d.name,url,description:d.description,applicationCategory:'UtilitiesApplication',operatingSystem:'Web browser',isAccessibleForFree:true,offers:{'@type':'Offer',price:'0',priceCurrency:'USD'}},{'@type':'FAQPage',mainEntity:d.faqs.map(([name,text])=>({'@type':'Question',name,acceptedAnswer:{'@type':'Answer',text}}))}]}));
 $('main').html(`<nav class="breadcrumb" aria-label="Breadcrumb"><a href="/">Home</a> / <a href="/#category-2">Calculators</a> / ${d.name}</nav><section class="tool-heading"><p class="overline">CLEAR INPUTS. USEFUL ANSWERS.</p><h1>${d.h1}</h1><p>${d.intro}</p></section>
<section id="workspace" class="workspace tip-workspace" data-tool="${d.slug}" aria-label="${d.name}"><form id="shopping-form" novalidate autocomplete="off"><div class="tip-grid">
${isDiscount?'':'<label class="tip-wide">Calculation<select id="mode"><option value="add">Add tax to a price</option><option value="remove">Remove tax already included</option></select></label>'}
<label><span id="price-label">${isDiscount?'Original listed price':'Price before tax'}</span><input id="price" type="text" inputmode="decimal" maxlength="11" placeholder="100.00" aria-describedby="price-help" required><span id="price-help" class="hint">Up to 1,000,000. Use a dot or comma for decimals, without thousands separators.</span></label>${currency}
<label>${isDiscount?'First discount':'Tax rate'} (%)<input id="rate" type="text" inputmode="decimal" maxlength="8" placeholder="${isDiscount?'20':'Enter your rate'}" required><span class="hint">0–100%, with up to four decimal places.</span></label>
${isDiscount?'<label>Extra discount (%) — optional<input id="extra" type="text" inputmode="decimal" maxlength="8" placeholder="0"><span class="hint">Applied to the price after the first discount.</span></label><label class="tip-wide">Fixed reduction — optional<input id="voucher" type="text" inputmode="decimal" maxlength="11" placeholder="0.00"><span class="hint">Subtracted after percentage discounts.</span></label>':''}
</div><p class="tip-note">${isDiscount?'Check the promotion terms before combining discounts. Tax and shipping are not added by this calculator.':'Enter the applicable rate yourself. The calculator does not look up local tax rules or determine exemptions.'}</p><div class="calc-controls"><button type="submit" class="primary">${isDiscount?'Calculate savings':'Calculate tax'}</button><button type="reset">Reset</button></div><p id="error" class="error" role="alert"></p></form><div id="result" class="result" role="status" aria-live="polite"></div><div class="calc-controls"><button id="copy-result" type="button" disabled>Copy summary</button></div><p id="copy-status" class="hint" role="status"></p><noscript>Enable JavaScript to calculate locally.</noscript></section>
<section class="help"><div><h2>How to Use</h2><ol>${d.steps.map(s=>`<li>${s}</li>`).join('')}</ol></div><div><h2>Why Use This Tool</h2><ul>${d.benefits.map(s=>`<li>${s}</li>`).join('')}</ul></div></section><section class="explainer"><h2>How this ${isDiscount?'discount calculator':'sales tax and VAT calculator'} works</h2><p>${d.explainer}</p><p>Amounts use two decimal places. Calculations use integer arithmetic and round half a cent upward. The result is a calculation from your inputs, not a verified merchant quote or invoice.</p></section>
<section class="explainer"><h2>Frequently Asked Questions</h2>${d.faqs.map(([q,a])=>`<details><summary>${q}</summary><p>${a}</p></details>`).join('')}</section><section class="related"><h2>Related Tools</h2>${d.related.map(([s,n])=>`<p><a href="/tools/${s}">${n}</a></p>`).join('')}</section><section class="explainer"><h2>Check your next price</h2><p><a href="#workspace">Use the ${isDiscount?'discount calculator':'sales tax and VAT calculator'} above to see your breakdown.</a></p></section>`);
 await writeFile(`public/tools/${d.slug}.html`,$.html());
}
const home=load(await readFile('public/index.html','utf8'));for(const d of definitions)if(!home(`a[href="/tools/${d.slug}"]`).length)home('#category-2 .tool-grid').append(`<a class="tool" href="/tools/${d.slug}"><span class="tool-icon" aria-hidden="true">%</span><h3>${d.name}</h3><p>${d.slug==='discount-calculator'?'Calculate sale prices, stacked discounts and total savings.':'Add a custom tax rate or separate tax included in a total.'}</p><span class="tool-link">Open tool <span aria-hidden="true">↗</span></span></a>`);home('section[id^="category-"]').each((_,el)=>{const s=home(el),n=s.find('.tool-grid > a.tool').length;if(n)s.find('h2 span').text(String(n).padStart(2,'0'));});await writeFile('public/index.html',home.html().replaceAll('39 free','41 free').replaceAll('39 tools','41 tools').replaceAll('Thirty-nine','Forty-one'));
for(const slug of ['tip-calculator','percentage-calculator']){const file=`public/tools/${slug}.html`,page=load(await readFile(file,'utf8'));page('.related').html('<h2>Related Tools</h2>'+definitions.map(d=>`<p><a href="/tools/${d.slug}">${d.name}</a></p>`).join('')+'<p><a href="/tools/simple-calculator">Simple Calculator</a></p><p><a href="/tools/currency-converter">Currency Converter</a></p>');await writeFile(file,page.html());}
let sitemap=await readFile('public/sitemap.xml','utf8'),checker=await readFile('scripts/maintenance-check.mjs','utf8');for(const d of definitions){const route='/tools/'+d.slug;if(!sitemap.includes(route))sitemap=sitemap.replace('</urlset>',`<url><loc>https://kalikatools.com${route}</loc></url></urlset>`);if(!checker.includes('"'+route+'"'))checker=checker.replace('  "/tools/tip-calculator",',`  "/tools/tip-calculator",\n  "${route}",`);}await writeFile('public/sitemap.xml',sitemap);await writeFile('scripts/maintenance-check.mjs',checker);
